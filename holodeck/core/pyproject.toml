[project]
name = "holodeck"
version = "0.4.0"
description = "Portable interactive developer identity and provider setup"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
holodeck = "holodeck.cli:main"
