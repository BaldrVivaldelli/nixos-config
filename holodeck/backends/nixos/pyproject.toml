[project]
name = "holodeck-system-nixos"
version = "0.2.0"
description = "Optional NixOS-WSL installation backend for Holodeck"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
holodeck-system-nixos = "holodeck_system_nixos.cli:main"
